{
  "builds": [
    {
      "src": "mix.exs",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "priv/static"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ]
}
